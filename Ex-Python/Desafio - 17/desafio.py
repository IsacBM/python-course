# IsacBM - 2K23
from math import hypot
catop = float(input('Digite o valor: '))
catad = float(input('Digite o segundo valor'))

result = hypot(catop, catad)

print('A hipotenusa mede {:.2f}'.format(result))
